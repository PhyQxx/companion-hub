# 31 记忆时间线与历史回溯设计

> 文档类型：Active 详细设计
> 状态：第一版已实现并通过后端、前端构建与浏览器真实数据验收；L1 真实模型正/负历史回溯已通过，剩余为 14 天连续真实使用校准与后续非阻塞增强
> 日期：2026-08-20
> 上位架构：[02-功能设计.md](./02-功能设计.md)
> 关联：[10-任务资产与升级运维.md](./10-任务资产与升级运维.md)、[30-多主体持久化记忆设计.md](./30-多主体持久化记忆设计.md)

> 当前实现快照：`0011_timeline_event` 已落地，并对升级前 message/event 做历史回填；`TimelineStore`、completed turn/EventBus 持续索引、用户时区 `TemporalQueryParser`、`HistoryRecallService`、有界 Timeline Search、有限 Source Expansion、无证据 grounding、L2 索引壳、会话删除联动、`decision_meta.recall` 与 Admin Timeline API 均已接入并通过自动化回归。L1 真实模型已经验证“刚才”“昨天晚上”和无证据负例。真实回归进一步修正了四个边界：importance 不得在零相关时制造命中、中文 token 不拆成单字制造假相关、L2 小时间窗需补充 L2-only 索引壳、弱 Memory Top-K 不得覆盖 Timeline 的 no-evidence。Timeline Vue 管理工作区已完成 typecheck/build，并在浏览器以真实数据验证 178 条事件列表和 Source 原始消息下钻。设备聚合 retention、Memory Promotion 与后续反思任务不作为当前第一版阻塞项；连续使用质量记录统一进入 docs/32。

## 1. 目标

系统不要求 AI 永远把所有历史都放在“长期记忆”中。更自然的目标是：

> **重要的信息平时就记得；不重要的细节允许忘记；需要时可以根据时间、人物、设备、主题等线索回查历史证据。**

因此把“记忆”和“历史”分开：

- `Memory` 回答“什么值得长期记住”；
- `Timeline` 回答“过去发生过什么”；
- `Source` 保存真实原始证据；
- AI 可以明确表示“现在想不起来”，但在有线索时可以发起历史检索；
- 搜不到时必须承认没有证据，不得补写一个看似合理的答案。

---

## 2. 四层结构

```text
Working Context
  当前会话 / 当前状态
        ↓
Long-term Memory
  平时就记得的重要信息
        ↓
Timeline Index
  可按时间和语义翻阅的历史目录
        ↓
Source Records
  message / event / device / tool 等原始事实源
```

### 2.1 Working Context

- 最近对话消息；
- 当前回合状态；
- 当前环境最小快照；
- 当前可用设备/工具能力；
- 生命周期为当前会话或短时间窗口。

### 2.2 Long-term Memory

由 `30` 定义：

- 主体：`user / assistant / shared`；
- 类型：`semantic / preference / commitment / episodic / emotional`；
- 稳定槽位 `fact_key`；
- 来源、冲突、纠错、版本链、隐私和删除闭环。

长期记忆不是完整历史仓库，只保存具有持续价值的信息。

### 2.3 Timeline Index

统一索引对未来可能有回溯价值的历史信息：

- 对话主题与消息引用；
- 设备状态变化和聚合事件；
- 工具执行结果；
- 日历/提醒事件；
- 感知引擎产生的持久语义事件；
- 系统中其他允许持久化的事实来源。

### 2.4 Source Records

原始事实仍保留在其领域表中，例如：

- `message`；
- `event`；
- 未来 device state / aggregate；
- tool execution；
- calendar/reminder 等外部连接记录。

Timeline 是索引，不应无条件复制全部原始正文。

---

## 3. TimelineEvent 数据模型

建议新增 `timeline_event`：

```text
id                  bigint/uuid
user_id             uuid

occurred_at         timestamptz
ended_at            timestamptz nullable

source_type         varchar
source_id           varchar

actor               varchar
event_type          varchar
conversation_id     uuid nullable

title               varchar nullable
summary             text

privacy_level       L0/L1/L2
importance          0..1

entities            jsonb
keywords            jsonb
metadata            jsonb

embedding           json/vector nullable
embedding_model     varchar nullable
embedding_version   varchar nullable

created_at          timestamptz
```

### 3.1 source_type

第一版控制在有限枚举：

```text
message
event
device
tool
calendar
system
```

### 3.2 actor

```text
user
assistant
device
system
external
```

### 3.3 event_type

使用命名空间形式，避免无限平铺：

```text
conversation.message
conversation.topic
device.power_on
device.power_off
device.state_changed
tool.executed
calendar.created
presence.arrived_home
system.reminder_triggered
```

### 3.4 索引

至少需要：

```text
(user_id, occurred_at)
(user_id, event_type, occurred_at)
(user_id, actor, occurred_at)
(source_type, source_id)
全文/词法索引
向量索引（启用 embedding 后）
```

---

## 4. 什么进入 Timeline

### 4.1 对话

`message` 本身已经带时间和会话 ID，因此第一版不需要为每一句话完整复制正文。

建议两种方式并存：

1. message 可直接作为 Source 被 HistoryRecall 搜索；
2. 对具有主题价值的对话片段生成 `conversation.topic` TimelineEvent，保存摘要和消息范围引用。

例如：

```text
21:30～21:42
conversation.topic
summary = 双方讨论周末观看《星际穿越》
source = conversation + message range
```

### 4.2 设备

禁止把高频遥测全部永久塞入 Timeline。

```text
raw telemetry
   ↓ 短期窗口/短期保留
aggregate
   ↓
meaningful event
   ↓
Timeline
```

示例：

- 每秒温度 24.31/24.32/24.31：不进 Timeline；
- 09:00～10:00 平均 24.5℃：可作为聚合数据中期保留；
- 09:37 温度快速升至 31℃：可生成 TimelineEvent。

具体 retention 仍以 `10` 为统一真源。

### 4.3 Tool / Calendar / System

只有成功完成且未来可能需要追溯的结果进入 Timeline。失败重试日志留在可观测系统，不应伪装成“发生过的用户事件”。

---

## 5. 历史回溯触发

普通聊天默认只使用 Working Context + Long-term Memory。只有满足以下条件之一才进入 History Recall：

1. 用户明确询问过去：`之前 / 昨天 / 上周 / 那次 / 以前 / 去年`；
2. Memory 没有答案，但问题明显依赖历史事实；
3. 用户提供时间、人物、设备、地点或主题线索并要求查找；
4. 模型需要验证一个可能来自历史的事实，而不是凭空补全。

不会因为“Memory 没命中”就每轮自动扫描全部历史。

---

## 6. HistoryRecallService

新增独立服务，不能把数据库搜索自由权直接交给模型。

```text
User Query
   ↓
Recall Intent Detection
   ↓
TemporalQueryParser
   ↓
RecallPlan
   ↓
Timeline/Search Source
   ↓
Evidence Set
   ↓
Grounded Context
```

建议接口：

```python
class HistoryRecallService:
    async def plan(...): ...
    async def search(...): ...
    async def expand_sources(...): ...
```

`RecallPlan` 至少包含：

```text
query
start_at / end_at
actors
event_types
source_types
entities
conversation_id
max_results
```

---

## 7. TemporalQueryParser

时间范围是一等公民，不只是 embedding 文本的一部分。

第一版支持：

```text
刚才
今天早上 / 下午 / 晚上
昨晚
前几天
上周 / 上周末
这个月 / 上个月
去年
```

全部转换为用户时区下的绝对 `start_at/end_at`。

第二阶段支持锚点时间：

```text
我回家以后
吃饭之前
上次打开电视的时候
```

流程是先搜索锚点事件，再生成相对时间窗口。

---

## 8. 历史检索策略

Timeline 排序综合：

- 时间范围匹配；
- lexical / FTS；
- embedding 语义相关度；
- entity 命中；
- actor / event_type；
- importance。

第一版不固定永久权重，通过 P5/P6 的真实数据校准。

### 8.1 渐进扩大范围

禁止无边界扫库。

例如“前阵子说的那个游戏”：

1. 先查 30 天；
2. 无结果则扩大至 90 天；
3. 仍无结果时，优先向用户询问更具体线索；
4. 单回合 Timeline Search 最多 2～3 次。

---

## 9. Source Expansion

Timeline 命中只是候选目录。需要准确回答时，再根据 `source_type/source_id` 回原始领域表取有限范围证据。

例如：

```text
Timeline: 21:30～21:42 讨论电影
   ↓
Source Expansion
   ↓
读取该范围 4～8 条 message
   ↓
构造证据上下文
```

不把整段会话、整天设备数据全部塞回 LLM。

---

## 10. 回答模式与可观测性

每轮回答增加内部 `recall_mode`：

```text
working     当前上下文即可回答
memory      长期记忆命中
timeline    通过历史索引找到
source      进一步读取原始证据后回答
none        没有足够证据
```

建议写入：

```json
{
  "recall": {
    "mode": "timeline",
    "timeline_ids": [9182, 9189],
    "memory_ids": [],
    "time_range": {
      "start_at": "...",
      "end_at": "..."
    },
    "search_count": 1
  }
}
```

日志只记录 ID、范围、原因码和耗时，不复制敏感正文。

---

## 11. “忘记”是允许的

系统必须区分三种体验：

### 11.1 平时就记得

来自 Memory：

> 你不吃香菜，我记得。

### 11.2 一时想不起，但可以翻历史

> 我没有把这个当成长期记忆，不过你说大概是上周。我查了一下，是上周三晚上我们聊到的《星际穿越》。

### 11.3 没有证据

> 我查了那段时间的记录，没有找到能确定的内容。你如果还记得大概月份或当时的话题，我可以再缩小范围。

禁止第四种行为：**Memory 没有、Timeline 没有、Source 也没有，但模型编出一个答案。**

---

## 12. Memory Promotion

新增 `MemoryPromotionService`，把 Timeline 中反复出现或明显重要的信息转成 MemoryCandidate。

### 12.1 高优先级

- 用户明确“记住……”；
- 稳定身份事实；
- 稳定偏好；
- 明确承诺；
- 重要关系事件；
- 经过多次独立证据支持的稳定模式。

### 12.2 默认只留 Timeline

- 今天午饭吃了什么；
- 临时看到什么；
- 一次性设备状态；
- 普通闲聊细节；
- 没有未来价值的偶发信息。

### 12.3 从历史形成印象

例如连续数周的 Timeline 都显示“睡前打开卧室风扇”，反思任务可提出：

```text
user preference / routine candidate:
用户睡前通常会打开卧室风扇
```

候选仍必须经过现有 MemoryIngester、冲突和隐私规则，不能直接写成 active 事实。

---

## 13. 与多主体记忆的关系

`30` 与本文不是替代关系：

```text
30：长期记忆描述谁、事实如何稳定
31：什么要长期记、忘了以后如何回查
```

长期记忆继续区分：

```text
user
assistant
shared
```

Timeline 则更偏向事件视角，记录 `actor`、参与者、来源和时间。

---

## 14. 隐私与数据保留

隐私等级仍采用项目 L0～L3：

| 隐私 | Timeline | Memory | 原始数据 |
|---|---|---|---|
| L0 | 可按策略持久化 | 可 | 按 retention |
| L1 | 用户域内可持久化 | 可 | 按 retention |
| L2 | 仅允许策略批准的脱敏事件/摘要 | 可脱敏沉淀 | 强制本地处理 |
| L3 | **禁止持久化** | **禁止** | 仅实时有界内存 |

设备的 `raw_retention / aggregate_policy / timeline_policy` 归 `10` 的数据保留体系管理，本文只定义进入 Timeline 的语义。

---

## 15. 删除闭环

Timeline 不能成为删除后的数据旁路。

删除 Source 时必须重新处理相关：

```text
Source
  ↓
Timeline index
  ↓
Memory source refs / derived memory
```

要求：

- TimelineEvent 具有可追溯 `source_type/source_id`；
- 删除会话时清理或失效相应 Timeline；
- 如果某 Memory 还有其他独立来源，不因删除单一来源误删整条事实；
- deletion ledger 扩展到 timeline 实体；
- 备份恢复后重放删除台账，防止 Timeline 索引复活。

---

## 16. ChatService 集成目标

```text
收到用户消息
  ↓
Working Context
  ↓
Long-term Memory Retrieve
  ↓
是否需要历史回溯？
  ├─ 否 → 继续
  └─ 是 → HistoryRecallService
             ↓
          Timeline
             ↓
        Source Expansion（必要时）
  ↓
Grounded Context
  ↓
LLM
  ↓
MemoryConsistencyGuard
  ↓
回复
  ↓
异步 Timeline / Memory 沉淀
```

记忆或历史子系统故障都不能导致已经可以正常回答的普通聊天失败。

---

## 17. API 草案

### 管理检索

```text
POST /api/v1/admin/timeline/query
GET  /api/v1/admin/timeline
GET  /api/v1/admin/timeline/{id}
```

Query：

```json
{
  "user_id": "...",
  "query": "电影",
  "start_at": "2026-08-18T18:00:00+08:00",
  "end_at": "2026-08-19T00:00:00+08:00",
  "actors": ["user", "assistant"],
  "source_types": ["message"],
  "event_types": [],
  "limit": 20
}
```

聊天运行时不直接暴露“任意数据库查询”给模型，只调用受控 HistoryRecallService。

---

## 18. 管理后台

记忆后台最终分为两个工作区：

### 18.1 长期记忆

- user / assistant / shared；
- fact_key；
- 来源；
- 冲突；
- 编辑、归档、删除。

### 18.2 时间线

- 时间轴；
- 来源类型；
- actor；
- event_type；
- 关键词/语义搜索；
- 隐私等级；
- Source 下钻；
- “从本事件创建记忆候选”的受控操作。

---

## 19. 实现顺序

### 19.0 当前实现进度（2026-08-20）

- [x] Phase A 后端：`0011_timeline_event`、旧 message/event 回填、`TimelineStore`、completed turn/EventBus 索引、时间范围查询与 Admin 调试 API；
- [x] Phase B 后端：历史意图、用户时区 `TemporalQueryParser`、`RecallPlan`、有界搜索/一次受控扩展、Source Expansion、无证据 grounding、`decision_meta.recall`；
- [x] L1 真实模型回归：刚才/昨天晚上正例与“有同时间段无关历史、无关长期 Memory”的 no-evidence 负例均已通过；
- [x] Timeline Vue 管理工作区：支持 user/time/query、actor/source/event_type、privacy、conversation 过滤，事件详情与受控 Source 下钻；typecheck/build 已通过，浏览器真实数据验收完成（178 条事件、Source 原始消息下钻）；
- [ ] Phase C 设备聚合与 retention 接线；
- [ ] Phase D Memory Promotion / 反思任务；
- [ ] Phase F 连续真实使用验收（随 docs/32 的 14 天 P5 使用期进行）。

### Phase A：Timeline 基础

```text
timeline_event migration
TimelineStore
message/event 索引
时间范围查询
管理调试 API
```

### Phase B：History Recall

```text
历史意图检测
TemporalQueryParser
RecallPlan
渐进式 Timeline Search
decision_meta.recall
```

### Phase C：设备时间线

```text
设备聚合
retention policy 接线
semantic event → Timeline
```

### Phase D：Memory Promotion

```text
Timeline → MemoryCandidate
显式“记住”优先级
重复模式
反思任务
```

### Phase E：与 docs/30 合流

```text
user / assistant / shared
fact_key exact recall
conflict
consistency guard
```

### Phase F：真实使用验收

重点测试模糊时间、跨会话、跨设备、删除、L2/L3、找不到和错误线索。

---

## 20. 第一版最小可落地范围

第一版后端最小范围已经完成：

1. [x] message/event 均能按时间检索；
2. [x] 建立最小 TimelineStore；
3. [x] 支持 `query + start_at + end_at + source_type`；
4. [x] 识别“昨天 / 上周 / 之前”等历史意图并按用户时区解析；
5. [x] 用户明显在问历史时受控查 Timeline；已有可靠 Memory 命中时不被空 Timeline 反向否定；
6. [x] Timeline 命中后只扩展有限 Source；
7. [x] 查不到时注入“无证据不可编造”约束；
8. [x] `decision_meta.recall` 可追踪 working / memory / timeline / source / none 语义。

不在第一版实现完整知识图谱、无限事件分类或复杂自主研究式检索。

---

## 21. 验收用例

- [x] “我昨天晚上说想看的电影是什么？”可按用户时区找到历史消息并下钻原始 Source；
- [x] 不是长期记忆的普通细节，仍可在给出大致时间后跨会话回查；
- [x] 没有时间线证据时注入明确的“不编造”约束；
- [x] 模糊历史范围禁止无边界扫库；当前最多做一次有界扩展，否则要求更多线索；
- [x] Timeline 命中可下钻到 Source ID；
- [x] 删除会话 Source 后对应 Timeline 不再可检索；
- [x] L3 从未进入 Timeline；
- [x] L2 Timeline 不进入 L0/L1 上下文；L2 索引不复制敏感正文，只在 L2 回合受控展开 Source；
- [x] 普通聊天没有历史意图时 Timeline Search 调用数为 0；
- [x] `decision_meta.recall` 已分别覆盖 working / memory / timeline / source / none。
- [x] Admin 调试 API 支持 Timeline 详情与受控 Source 原始证据下钻；Vue 管理页已接入，typecheck/build 与浏览器真实数据验收均已完成。

