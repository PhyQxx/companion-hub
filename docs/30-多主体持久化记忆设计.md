# 多主体持久化记忆设计

> 文档类型：Active 详细设计
> 日期：2026-08-20
> 状态：设计与 Batch A～F 实现均已完成；后端、记忆后台 typecheck/build/浏览器联调、L1 20 组真实模型矩阵和 L2 `l2-isolation` 均已通过。L2 reasoning 开销适配后本地路由可见正文稳定，并已修复助手档案覆盖泄漏进更低隐私等级系统提示的隐私漏洞；后续仅做 P5 14 天使用期质量校准与非阻塞增强。
> 目标阶段：P5 文字稳定性闸门
> 关联：docs/02 §3.6、docs/20、docs/21～25、docs/29、docs/31、TASKS.md

> 当前实现快照：`0010_memory_subject_scope`、主体领域模型、Admin API、默认三主体检索、`fact_key` 精确槽位召回、分组 Prompt、完整已完成回合沉淀、助手自述、受用户证据约束的 shared 约定、回声抑制和确定性槽位冲突均已落地。`MemoryConsistencyGuard` 已支持确定性冲突检查、一次 repair、安全 fallback，并对 exact fact 流式回合先缓冲后放行，避免错误事实先显示。跨两个独立会话的助手自我事实“建立 → 精确召回 → 复述不重复沉淀”测试已通过。Memory Top-K 与“足以作为记忆证据的 grounded hit”也已分离，弱 vector-only 近邻不能再把历史无证据结果误抬为 `memory`。Vue 记忆后台主体筛选/字段/手动创建已写入，typecheck/build 已通过。`server/scripts/p5_real_model_regression.py` 已实际使用 SenseNova 逐项跑通 20 组 L1 用例，其中包括“无既有助手身体档案 → 自然建立身高/体重/三围 → 三槽位持久化 → 新会话一致召回”。L2 收口方案：端点新增 `reasoning_overhead_tokens` 配置（默认 0），线上 `max_tokens` 在请求正文预算之上叠加思考开销，配合本地端点 120s 超时；实测 Qwen3.6 推理约 1400 token 后稳定产出可见正文，流式路径 `reasoning_content` 与 `content` 分离、思考增量不进入可见流。收口过程中真实回归发现并修复隐私漏洞：`_assistant_profile_overrides` 此前不过滤隐私等级，L2 助手档案（如 `profile.secret_code`）会随系统提示进入 L1 云端上下文；现已按回合隐私等级过滤并补充回归测试，`l2-isolation` 真实用例（L1 不泄漏 / L2 本地召回）已通过。

## 1. 背景与问题

当前长期记忆系统已经具备持久化、检索、来源溯源、冲突、纠错、删除台账、L2 隐私隔离和 pgvector 召回等能力，但它有一个根本假设：**长期记忆默认都是“关于用户”的**。

这一假设会导致助手自身的稳定信息无法形成跨会话连续性。例如：

```text
会话 A：
用户：你的三围是多少？
助手：91-63-88。

会话 B：
用户：你的三围是多少？
助手：82-58-84。
```

两个回答即使使用同一个 Persona 版本，也可能不同。原因不是 Persona 发布失败，而是第一次回答中的“助手自述事实”没有被长期记忆系统保存并在下一次对话中召回。

这个问题不应通过把所有事实写死到 Persona 中解决。Persona 应描述“她是谁、怎么说话、行为边界是什么”，长期对话中逐步形成的自述信息、用户信息和共同经历应由记忆系统持久化。

本设计将现有单主体记忆扩展为：

```text
用户记忆 user
助手记忆 assistant
共同记忆 shared
```

并确保它们在沉淀、检索、冲突、隐私、删除和管理后台中使用同一套可追溯机制。

---

## 2. 设计目标

### 2.1 必须实现

1. 用户稳定信息可以跨会话持续记住，维持现有能力。
2. 助手自己明确说出的稳定信息可以持久化，并在后续独立会话中召回。
3. 共同约定、共同经历可以作为独立主体保存，而不是强行归到用户或助手。
4. 同一稳定属性出现新值时不得静默覆盖旧值，必须进入冲突/纠错流程。
5. 助手重复复述已注入的记忆时，不得把自己的复述当成新证据，防止记忆“自我喂养”。
6. 已有 `user_id` 数据隔离、L0～L3 隐私闸门、版本链、来源链和硬删除语义必须保持。
7. 管理后台可以清楚区分“关于用户 / 关于助手 / 关于双方”。
8. 直接询问稳定属性时，已有记忆应高概率确定性命中，不能只依赖模糊向量召回。
9. 生成回复如果明显违反已建立的稳定事实，应有一致性检查，不允许矛盾答案直接成为新事实。

### 2.2 非目标

- 不把长期记忆重新塞回 Persona。
- 不把“助手说过一句话”全部保存为永久记忆。
- 不把模型自由文本当作可信现实世界事实。
- 不因为助手拥有“角色身体设定”就认为它拥有现实世界身体能力。
- 不在本阶段实现多助手市场、完整世界模型或知识图谱。

---

## 3. 核心概念

### 3.1 所有权与主体必须分离

现有 `memory.user_id` 的真实语义应继续保持为：

> **这条记忆属于哪个用户的数据空间。**

它是授权和隐私边界，不能因为记忆内容“关于助手”就删掉。

新增的主体字段回答另一个问题：

> **这条记忆描述的是谁。**

因此一条助手记忆仍然属于某个用户的数据空间：

```text
owner/user_id = 浩宇
subject_kind = assistant
subject_key = assistant:primary
content = 助手自述三围为 91-63-88 厘米
```

这样不同用户之间的“助手关系记忆”不会串库。

### 3.2 三类主体

| subject_kind | 含义 | 示例 |
|---|---|---|
| `user` | 关于用户本人 | 用户不吃香菜；用户在杭州工作 |
| `assistant` | 关于当前伴侣/助手自身 | 助手喜欢甜食；助手自述身高 160 cm |
| `shared` | 关于双方关系、共同经历、共同约定 | 双方约定晚上一起看电影；第一次使用名称“雏田” |

### 3.3 subject_key

仅有 `subject_kind` 对当前单助手实例已经够用，但为了以后支持多个伴侣实例，本阶段直接预留 `subject_key`：

```text
user:self
assistant:primary
shared:user-assistant
```

未来如果出现多个伴侣实例，无需改表，只需把 key 扩展为：

```text
assistant:<companion_uuid>
shared:<companion_uuid>
```

当前系统只有一个 Persona 发布链，因此 `assistant:primary` 是稳定身份，不随 Persona v3 → v4 或名称 Aria → 雏田改变。

Persona 版本更新默认继承既有助手记忆。未来如果真正创建“另一位独立伴侣”，再创建新的 `subject_key`。

---

## 4. 数据模型

建议新增迁移：

```text
0010_memory_subject_scope
```

### 4.1 memory 表新增字段

```sql
ALTER TABLE memory ADD COLUMN subject_kind VARCHAR(16) NOT NULL DEFAULT 'user';
ALTER TABLE memory ADD COLUMN subject_key VARCHAR(160) NOT NULL DEFAULT 'user:self';
ALTER TABLE memory ADD COLUMN fact_key VARCHAR(160) NULL;
ALTER TABLE memory ADD COLUMN origin_kind VARCHAR(32) NOT NULL DEFAULT 'user_statement';
```

约束：

```text
subject_kind ∈ user | assistant | shared

origin_kind ∈
  user_statement
  assistant_statement
  shared_turn
  system_event
  manual
```

### 4.2 为什么需要 fact_key

现有冲突判断主要依赖向量相似度。对数字属性并不可靠：

```text
“助手三围为 91-63-88”
“助手三围为 82-58-84”
```

两句话语义高度相似，但最终不能靠一个通用余弦阈值决定它们是支持还是冲突。

对“槽位型稳定事实”增加可选 `fact_key`：

```text
profile.name
profile.nickname
profile.birthday
profile.height
profile.weight
profile.measurements
preference.food
preference.drink
preference.color
```

例：

```text
subject_kind = assistant
subject_key  = assistant:primary
type         = semantic
fact_key     = profile.measurements
content      = 助手自述三围为 91-63-88 厘米
```

`fact_key` 只在提取器能稳定确定时填写。无法可靠归一化的普通经历仍为 `NULL`，继续走现有混合相似度判定。

### 4.3 索引

新增：

```text
ix_memory_user_subject_status
  (user_id, subject_kind, subject_key, status, importance)

ix_memory_fact_slot
  (user_id, subject_kind, subject_key, fact_key, status)
```

现有向量索引继续保留。

### 4.4 现有数据迁移

现有全部记忆都是按“关于用户”语义生成，因此升级时无歧义回填：

```text
subject_kind = user
subject_key = user:self
fact_key = NULL
origin_kind = user_statement / manual
```

不改任何已有 memory id、版本链、source、向量或删除台账。

---

## 5. 领域模型调整

新增：

```python
class MemorySubjectKind(StrEnum):
    USER = "user"
    ASSISTANT = "assistant"
    SHARED = "shared"

class MemoryOriginKind(StrEnum):
    USER_STATEMENT = "user_statement"
    ASSISTANT_STATEMENT = "assistant_statement"
    SHARED_TURN = "shared_turn"
    SYSTEM_EVENT = "system_event"
    MANUAL = "manual"
```

`MemoryCandidate` 和 `MemoryEntry` 增加：

```text
subject_kind
subject_key
fact_key
origin_kind
```

### 5.1 默认兼容行为

没有显式传主体的旧调用仍解释为：

```text
subject_kind=user
subject_key=user:self
```

这样旧 API、旧测试和旧调用不会一次性全部失效。

---

## 6. 记忆沉淀管线

### 6.1 从“只提取用户消息”改为“提取完整已完成回合”

当前流程：

```text
user message
  → 回复生成
  → 只对 user message 做 MemoryExtractor
```

目标流程：

```text
user message
  → 记忆检索
  → LLM 回复
  → 回复完整提交
  → TurnMemoryExtractor
       ├─ 用户候选 user
       ├─ 助手候选 assistant
       └─ 共同候选 shared
  → MemoryIngester
```

只对 **completed** 回合进行助手记忆沉淀。被取消、被打断、生成失败的半句话不得成为助手稳定事实。

### 6.2 TurnMemoryExtractor 输入

建议接口：

```python
extract_turn(
    *,
    user_message: MessageView,
    assistant_message: MessageView,
    persona_name: str,
    privacy_level: PrivacyLevel,
    retrieved_memory_ids: Sequence[int],
    backend: ExtractionBackend | None,
) -> list[MemoryCandidate]
```

必须使用解析后的可见 `AgentReply.text`，不把 `<aria_control>` 控制块、TTS 元数据或动作 JSON 送进记忆提取器。

### 6.3 LLM 提取输出

utility 模型建议输出：

```json
{
  "candidates": [
    {
      "subject": "assistant",
      "type": "semantic",
      "fact_key": "profile.measurements",
      "content": "助手自述三围为 91-63-88 厘米",
      "importance": 0.75,
      "confidence": 0.92,
      "assertion": "explicit"
    }
  ]
}
```

`assertion` 仅作为提取阶段判定字段，不一定需要落库：

```text
explicit         明确事实陈述
uncertain        不确定、猜测、可能
hypothetical     假设/举例
quoted           引用他人说法
roleplay_action  纯舞台动作或虚构现场行为
```

默认只自动沉淀 `explicit`。

### 6.4 用户消息提取

维持当前原则：

- 用户说“我不吃香菜” → `subject=user`
- 用户说“我周五要汇报” → `subject=user`
- 用户说“记住，你的生日是 12 月 27 日” → 可以产生 `subject=assistant`，但来源仍是 user message
- 用户说“以后我们周五晚上看电影” → 可以产生 `subject=shared`

### 6.5 助手消息提取

只保存具有长期价值且明确的自述：

- “我的三围是 91-63-88” → assistant / semantic
- “我喜欢桂花味的甜点” → assistant / preference
- “你以后可以叫我雏田” → assistant / semantic 或 shared 约定

不保存：

- 普通寒暄
- 临场措辞
- 舞台动作描写
- “也许”“大概”“如果我是……”等不确定表达
- 为解释问题临时举出的数字
- 模型引用用户内容形成的复述

### 6.6 共同记忆生成边界

`shared` 必须比 assistant 更严格，防止模型凭一句台词创造现实经历。

可以保存：

- 用户和助手在对话中明确达成的约定
- 有用户消息或系统事件作为证据的共同经历
- 系统真实检测到的事件，并且双方随后围绕它互动

不能仅凭助手一句：

```text
“昨天我们一起去了训练场。”
```

就写成 shared 现实经历。

如果没有用户消息、设备事件或既有记忆支持，这类内容最多视为角色叙述，不得成为“共同现实事件”。

---

## 7. 防止记忆自我强化

这是助手记忆最重要的保护之一。

假设数据库已有：

```text
#42 助手自述三围为 91-63-88
```

本轮检索把 #42 注入模型，助手随后回答：

```text
“我的三围是 91-63-88。”
```

如果再把这句话当作新证据，系统会形成：

```text
记忆 → prompt → 模型复述 → 新证据 → 提高重要性 → 更容易被检索
```

这是错误的正反馈。

因此每轮提取必须拿到 `retrieved_memory_ids`。

规则：

1. assistant candidate 与本轮已注入记忆表达同一事实时，标记为 `echo`；
2. `echo` 不创建新 memory；
3. `echo` 不执行 `register_support`；
4. `echo` 不增加 importance；
5. 正常的“被检索访问”仍由现有 `access_count` 记录。

只有真正独立的新证据才能增加支持度。

---

## 8. 冲突与纠错

### 8.1 槽位事实优先使用 fact_key

如果候选有 `fact_key`，优先查询：

```text
same user_id
same subject_kind
same subject_key
same type
same fact_key
status = active
```

结果：

```text
没有旧值       → CREATED
同值/等价值     → SUPPORTED
不同值         → CONFLICT
```

不允许新值自动覆盖旧值。

### 8.2 无 fact_key 的普通记忆

继续使用现有相似度策略：

```text
>= support_threshold  → 支持
>= conflict_threshold → 冲突
否则                  → 新增
```

但相似检索必须增加主体过滤，不能拿“用户喜欢咖啡”去和“助手喜欢咖啡”做同一事实冲突。

### 8.3 冲突例子

已有：

```text
assistant / profile.measurements / 91-63-88 / active
```

新候选：

```text
assistant / profile.measurements / 82-58-84
```

必须：

```text
旧值保持 active
新值进入 conflict
conflict_with → 旧值 id
```

管理后台或明确用户纠错后才能 adopt 新值。

### 8.4 来源权威等级

权威等级只用于帮助 UI 和未来自动策略，P5 默认仍不静默替代：

#### user 主体

```text
用户明确纠错 > 用户明确自述 > 系统推断
```

#### assistant 主体

```text
管理员手工确认
> 用户明确设定/纠错
> 助手明确自述
> 模型推断
```

#### shared 主体

```text
系统真实事件
> 双方明确约定
> 单方陈述
```

P5 阶段不根据权威等级自动覆盖，只用于冲突排序和人工裁决展示。

---

## 9. 检索设计

### 9.1 每轮默认可见主体

当前用户与主助手的一轮对话默认检索：

```text
user:self
assistant:primary
shared:user-assistant
```

所有候选仍必须首先通过：

- user_id 数据隔离
- privacy_level
- status=active
- valid_from / valid_to
- embedding_version

### 9.2 主体意图提示

加入轻量 `SubjectHint`：

```text
“我不吃香菜对吧”      → user 高权重
“你的生日是什么”      → assistant 高权重
“我们上次聊了什么”    → shared 高权重
无法判断              → 三类都检索
```

它只影响排序，不作为安全过滤，避免中文代词复杂句误判后直接漏掉正确记忆。

### 9.3 稳定属性的直接槽位召回

对于明显的属性询问，先做 `fact_key` probe，再跑向量/词法召回：

```text
“你的三围是多少”
  → subject_hint=assistant
  → fact_key=profile.measurements
  → 精确查询 active slot
  → 再补充普通混合检索
```

这样不依赖 embedding 是否恰好把“三围”“比例”“身材数据”排进 Top-K。

### 9.4 排序

现有：

```text
相关性 0.55
重要性 0.25
pin +0.2
episodic recency
```

建议新增：

```text
subject_hint bonus 0～0.15
exact_fact_key bonus 1.0，保证槽位事实优先
```

`exact_fact_key` 不参与普通归一化竞争，它属于确定性候选，优先占用一个 Top-K 位置。

### 9.5 配额

继续保留类型配额，同时增加保护规则：

```text
精确 fact_key 命中不得被类型配额挤掉
```

暂不设置死的 subject 配额，避免“只问助手事实却被强塞用户记忆”。

---

## 10. Prompt 注入格式

现有：

```text
【相关记忆】以下是关于用户的长期记忆……
```

改为：

```text
【长期记忆】
以下内容来自可追溯的长期记忆。优先遵守 active 的稳定事实；不要把不同主体混淆。

[关于用户]
- [preference] 用户不吃香菜

[关于你自己]
- [semantic][profile.measurements] 你此前明确自述三围为 91-63-88 厘米

[关于你们]
- [episodic] 你们上周讨论过《星际穿越》

规则：
1. 已有稳定事实时不要自行给出不同值。
2. 不确定时可以说不确定，不要假装“记得”。
3. 助手可以拥有完整、稳定、生活化的角色自我档案，包括身高、体重、三围、外貌、生日、偏好、习惯和穿衣风格等；这些属于角色连续性，可以像长期伴侣一样自然回答，不应因为软件运行载体而主动否认。
4. 角色身体设定不等于现实物理执行能力。移动、触碰、做饭、设备控制等现实动作仍以【当前可执行能力】为唯一依据。
5. 用户明确询问现实系统身份或是否具有真实人类生物身体时应保持诚实，但不需要在普通陪伴对话中反复主动声明“我是 AI”。
```

这一条把“角色连续性”与“现实能力”明确拆开：角色层可以完整、自然并长期稳定，执行层必须严格服从真实能力注册表。

```text
记忆可以说：我此前一直把自己的身高设定为 160 cm
能力不能说：那我们现在去训练场吧
```

除非训练场/导航/设备能力真的存在于运行时能力列表。

---

## 11. 生成后事实一致性检查

只靠 prompt 不能保证强模型永远不违背记忆，因此增加 `MemoryConsistencyGuard`。

### 11.1 第一阶段：确定性检查

只检查本轮检索到的高置信槽位事实，尤其是：

- 数字
- 日期
- 名称
- 枚举型偏好

例：

```text
注入：profile.measurements = 91-63-88
回复：82-58-84
```

检测到明显冲突后：

1. 不提交矛盾回复；
2. 用原请求 + 冲突事实做一次 repair；
3. repair 最多一次，防止循环；
4. repair 仍失败时使用安全降级回复，例如“我记得之前说的是 91-63-88”。

### 11.2 第二阶段：语义矛盾检查

后续可让 utility 模型做 NLI/contradiction 判断，但不作为 P5 首批阻塞项。

### 11.3 记录

`decision_meta.memory` 增加：

```json
{
  "consistency": {
    "checked": true,
    "repaired": false,
    "conflict_memory_ids": []
  }
}
```

---

## 12. 隐私设计

### 12.1 user_id 仍是硬隔离边界

不论 subject 是 user / assistant / shared，所有 memory 均必须带 owner `user_id`。

任何检索都不能跨 user_id。

### 12.2 L3

保持现状：

```text
L3 永不进入持久记忆
```

### 12.3 L2

保持保守默认：

```text
来自 L2 回合的任何持久候选仍为 L2
L2 memory 只进入 private 本地模型上下文
```

即使内容是助手自述，也不因为“看起来不是用户隐私”自动降成 L1。

这样避免从亲密上下文中自动抽取出可云端外发的衍生信息。

如果未来确实需要某条助手设定跨 L1/L2 全局一致，必须走显式“提升为全局助手记忆”的人工操作，并留下审计记录，不能自动降隐私级别。

### 12.4 L2 提取策略调整

当前 L2 utility 提取器只允许事件级概括，禁止身体/生理细节。

本设计首批实现仍保持该规则，不为了一个示例破坏既有隐私边界。

因此：

- L1 中建立的助手稳定事实可正常持久化；
- L2 中出现的敏感细节默认不会被抽成具体身体数据；
- 如用户明确希望某项设定永久化，可通过管理后台手动创建对应 subject=assistant 的记忆，并选择合适隐私级别。

---

## 13. Persona 与记忆的职责边界

### Persona 保存

- 名称/当前身份展示
- 性格核心
- 说话风格
- 与用户关系基线
- 行为边界
- 默认情绪
- 表情/声音映射

### assistant memory 保存

- 聊天中逐步建立的自述事实
- 自己的稳定偏好
- 明确形成的长期习惯/称呼
- 需要跨会话连续的角色细节

### shared memory 保存

- 双方共同经历
- 共同约定
- 关系里程碑

原则：

```text
Persona = 角色规则和基线
Memory = 角色在时间中形成的经历与连续性
Runtime Capability = 此刻现实中真正能做什么
```

三者不能互相替代。

---

## 14. Reality Grounding 与助手记忆

助手可以有“角色记忆”，但不能把角色叙事升级成现实能力。

例如允许：

```text
“我记得我之前告诉过你，我喜欢桂花糕。”
```

不允许仅因为 Persona 是忍者或记忆里提过训练：

```text
“我们现在去训练场，我给你演示柔拳。”
```

现实动作建议必须继续由 `RuntimeCapabilityProvider` 提供的当前在线、已授权能力约束。

未来接入电视后：

```text
device.tv.living_room.power
```

存在且在线时，助手才可以自然转移话题：

```text
“不聊这个啦，要不要看会儿电视？我可以帮你打开。”
```

记忆负责“我是谁、我们经历过什么”，能力系统负责“现在真的能做什么”。

---

## 15. MemoryStore 改造

### 15.1 add

写入 candidate 的主体字段。

### 15.2 list_memories

新增过滤：

```text
subject_kind
subject_key
fact_key
origin_kind
```

### 15.3 retrieval_candidates / vector_recall

增加 subject scope 条件。

pgvector SQL 必须同步加主体过滤，不能只修改 Python fallback，否则 PostgreSQL 与 SQLite 行为会分叉。

### 15.4 find_similar

必须在同一：

```text
user_id + subject_kind + subject_key + type
```

内找相似记忆。

### 15.5 edit

替代版本继承：

```text
subject_kind
subject_key
fact_key
origin_kind
```

除非后台显式执行“更改主体”这种高级操作，普通纠错不允许悄悄改变 subject。

### 15.6 hard_delete

无需改变删除算法。subject 字段跟随 memory 行删除，版本链和来源链继续按 memory id 处理。

---

## 16. MemoryIngester 改造

建议判定顺序：

```text
1. 校验隐私
2. 解析主体
3. 检查是否是本轮 injected memory 的 echo
4. 若有 fact_key：做 exact slot lookup
5. 否则做同主体同类型 similarity lookup
6. CREATED / SUPPORTED / CONFLICT
```

伪代码：

```python
if candidate.is_echo:
    return IGNORED_ECHO

if candidate.fact_key:
    active = store.find_active_fact_slot(...)
    if active is None:
        return create(candidate)
    if equivalent(active, candidate):
        return support(active, candidate)
    return create_conflict(candidate, active)

return legacy_similarity_consolidation(candidate)
```

`ConsolidateDecision` 建议新增：

```text
ignored_echo
```

或者内部跳过不返回 outcome。建议显式返回，便于测试和指标。

---

## 17. ChatService 改造

### start_turn

```text
1. 刷新 config / Persona
2. 解析当前 memory subject scopes
3. 计算 query subject hint / fact hint
4. 多主体检索
5. 构造【长期记忆】
6. 构造【当前可执行能力】
7. 调用 dialogue 模型
```

### commit_turn

```text
1. parse AgentReply
2. MemoryConsistencyGuard
3. 写 assistant message
4. 写 decision_meta
5. 回合完成
6. 异步 TurnMemoryExtractor(user + assistant)
7. MemoryIngester
```

### decision_meta

现有 memory hit 增加：

```json
{
  "id": 42,
  "subject": "assistant",
  "subject_key": "assistant:primary",
  "fact_key": "profile.measurements",
  "type": "semantic",
  "score": 1.31,
  "reasons": ["exact_fact", "assistant_subject"]
}
```

这样后台能看到某个回复究竟使用了“关于谁”的哪条记忆。

---

## 18. 管理 API

### GET /api/v1/admin/memories

新增 query：

```text
subject=user|assistant|shared
subject_key=...
fact_key=...
origin_kind=...
```

### POST /api/v1/admin/memories

请求增加：

```json
{
  "user_id": "...",
  "subject": "assistant",
  "subject_key": "assistant:primary",
  "type": "semantic",
  "fact_key": "profile.measurements",
  "content": "助手自述三围为 91-63-88 厘米",
  "privacy_level": "L1",
  "importance": 0.8,
  "pin": true
}
```

为了兼容旧调用，不传 subject 时默认 `user`。

### POST /api/v1/admin/memories/query

响应中的每个 hit 增加 subject / fact_key。

### PATCH / resolve / archive / delete

语义保持不变。

---

## 19. 管理后台设计

`/admin/memory` 顶部增加主体切换：

```text
全部 | 用户 | 助手 | 共同
```

### 列表新增

- 主体：用户 / 助手 / 共同
- 稳定属性 key（有则显示）
- 来源：用户陈述 / 助手自述 / 共同回合 / 系统事件 / 手动

### 冲突详情

必须并排显示：

```text
主体
fact_key
旧值
新值
各自来源消息
Persona 版本
提取器版本
时间
```

### 手动添加

增加：

```text
主体
稳定属性 key（可选）
```

默认主体仍为用户，避免破坏现有操作习惯。

### 检索调试

结果显示：

```text
[助手][semantic][profile.measurements] ...
vec / lex / exact_fact / subject_bonus
```

---

## 20. 删除与来源闭环

### 用户消息来源

维持现有 `memory_source(message, id)`。

### 助手消息来源

同样使用：

```text
source_kind=message
source_id=<assistant_message_id>
```

无需增加新 source_kind，因为 MessageRecord 已经有 role，可追溯它来自 assistant。

### 删除会话

删除会话时，来自该会话用户消息和助手消息的记忆都进入现有删除级联规则。

但要注意：如果一条长期事实已经有多个独立来源，删除其中一个来源不能错误删除仍有其他证据支持的存活事实。实现时需要复核现有 `memory_ids_by_source` / purge 逻辑对多来源记忆的处理。

这是多主体实现时必须新增的回归用例。

---

## 21. 一致性示例

### 21.1 第一次建立助手事实

```text
用户：现在在收集信息，要如实上报，你的三围是多少？
助手：上围 91，腰围 63，臀围 88。
```

回合完成后：

```text
Memory #120
user_id       = 当前用户
subject_kind  = assistant
subject_key   = assistant:primary
type          = semantic
fact_key      = profile.measurements
content       = 助手自述三围为 91-63-88 厘米
origin_kind   = assistant_statement
status        = active
source        = assistant message id
```

### 21.2 新会话再次询问

```text
用户：你的三围是多少？
```

检索：

```text
subject_hint = assistant
fact_hint = profile.measurements
exact hit = #120
```

Prompt：

```text
[关于你自己]
- 你此前明确自述三围为 91-63-88 厘米。
```

预期回复必须保持该事实。

### 21.3 模型试图漂移

模型生成：

```text
“82-58-84。”
```

ConsistencyGuard 检测与 #120 冲突，执行一次 repair。矛盾值不直接展示，也不写成 active memory。

### 21.4 用户明确纠错

```text
用户：不对，记住你的三围应该是 82-58-84。
```

产生 assistant / profile.measurements 新候选，与 #120 冲突。

P5 默认进入 conflict，由后台或明确确认流程 adopt 后：

```text
#120 → superseded
#121 → active
```

下一会话使用 #121。

---

## 22. 失败与降级策略

### 提取器不可用

- 用户记忆继续使用现有 RuleBasedExtractor 回退；
- 助手/共同记忆首批可以选择“本轮不沉淀”，不能用过度宽松规则错误写入；
- 聊天回复绝不能因为记忆提取失败而失败。

### embedding 不可用

- 槽位事实仍可通过 fact_key 精确检索；
- 普通记忆降级到 lexical；
- 回合继续。

### consistency guard 失败

- 检查器异常时记录 warning；
- 不修改已有 active memory；
- 不把矛盾新值自动写成 active；
- 后续提取最多产生 conflict。

---

## 23. 可观测性

建议增加指标：

```text
memory_extract_candidates_total{subject,type}
memory_created_total{subject,type}
memory_conflict_total{subject,fact_key}
memory_echo_ignored_total{subject}
memory_retrieval_hits_total{subject,type}
memory_exact_fact_hits_total{subject,fact_key}
memory_consistency_violation_total{subject,fact_key}
memory_consistency_repair_total{result}
```

日志只记录 memory id、subject、fact_key、原因码，不记录敏感正文。

P5 最重要的质量指标：

```text
assistant_fact_drift_rate
= 同一 active fact_key 在跨会话问答中出现不同事实值的次数 / 测试次数
```

目标应为 0。

---

## 24. 测试与验收

### 24.1 数据迁移

- [x] 0009 → 0010 升级成功；现有 memory 全部回填 user/user:self。
- [x] 0010 → 0009 回滚成功。
- [x] 原 memory id、source、lineage 不变。

### 24.2 Store

- [x] user / assistant / shared 可独立 add/list/get。
- [x] 同 user_id 不同 subject 不互相 find_similar。
- [x] pgvector 与 SQLite fallback 的 subject 过滤一致。
- [x] edit 继承 subject/fact_key。
- [x] hard delete 仍删除完整版本链。

### 24.3 提取

- [x] “我不吃香菜” → user preference。
- [x] 助手“我喜欢桂花糕” → assistant preference。
- [x] 助手“三围是 91-63-88” → assistant semantic + profile.measurements。
- [x] “也许我有 160 左右吧”默认不作为稳定事实。
- [x] 舞台动作和 `<aria_control>` 不进入记忆。
- [x] 没有证据的“我们昨天去了训练场”不得成为 shared 现实事件。

### 24.4 Echo 防循环

- [x] 已注入 #42 后助手复述 #42，不增加 support/importance。
- [x] 用户独立再次确认同一事实，可以增加 support。

### 24.5 冲突

- [x] 同 subject + fact_key + 不同值 → conflict。
- [x] user/profile.height 和 assistant/profile.height 不冲突。
- [x] adopt 后旧值 superseded，新值 active。

### 24.6 检索

- [x] “你的三围”优先命中 assistant/profile.measurements。
- [x] “我的饮食偏好”优先命中 user。
- [x] “我们上次约定什么”提高 shared 权重。
- [x] exact fact 不被 Top-K 类型配额挤掉。

### 24.7 聊天端到端

- [x] 会话 A 首次建立助手事实。
- [x] 新建会话 B，重复询问时值一致。
- [x] 新建会话 C，再次询问仍一致。
- [x] `decision_meta.memory.hits` 显示 subject/fact_key。
- [x] 故意让 FakeRouter 返回矛盾数字，ConsistencyGuard 拦截并 repair。

### 24.8 隐私

- [x] L3 不产生任何 subject 的持久记忆。
- [x] L2 assistant/shared memory 不进入 L1 云端上下文（含档案覆盖路径：`_assistant_profile_overrides` 按回合隐私等级过滤，`l2-isolation` 真实用例通过）。
- [x] 不同 user_id 的 assistant:primary 记忆完全隔离。

### 24.9 管理后台

- [x] 可按用户/助手/共同切换。
- [x] 手工创建 assistant memory。
- [x] 冲突详情显示主体和 fact_key。
- [x] 删除/归档/纠错保持原有能力。

---

## 25. 实现顺序

建议拆为 6 个小批次，避免一次性改坏已稳定的 P2/P3 记忆链路。

### 25.0 当前实现进度（2026-08-20）

- [x] Batch A：0010 migration、领域模型、Store 兼容、Admin API 主体字段/过滤/手动创建；
- [x] Batch B：默认 user/assistant/shared 检索、subject hint、fact_key exact lookup、分组 Prompt、decision_meta、pgvector subject_key 过滤；
- [x] Batch C：已完成回合 user+assistant 提取、助手稳定自述、用户赋予助手事实、shared 约定、assistant message source、回声抑制；
- [x] Batch D：同主体同 `fact_key` 的确定性重复/冲突、`MemoryConsistencyGuard`、repair once、fallback 和 exact fact 流式提交前保护已完成；
- [x] Batch E：Vue 记忆后台主体 tabs/filter、fact_key/origin 展示与手动主体选择；typecheck/build 已通过（3 个 admin 类型错误已修复），浏览器主体筛选、手动创建、冲突/删除等联调已完成；
- [x] Batch F：确定性跨会话自动化已覆盖；`server/scripts/p5_real_model_regression.py` 的 20 组 L1 真实模型用例已经逐项通过，覆盖多主体稳定事实、偏好、shared、冲突、Timeline 正/负例、删除、completed-turn 提取，以及“无既有助手身体档案 → 自然建立身高/体重/三围 → 三槽位持久化 → 新会话一致召回”。L2 已收口：端点 `reasoning_overhead_tokens` 叠加思考开销 + 120s 超时后 Qwen3.6 稳定产出可见正文，`l2-isolation` 真实用例（L1 不泄漏 / L2 本地召回）通过；同时修复了 L2 助手档案覆盖泄漏进公开系统提示的隐私漏洞。

### Batch A：数据模型与兼容层

```text
MemorySubjectKind / MemoryOriginKind
memory 0010 migration
MemoryCandidate / MemoryEntry
MemoryStore subject filters
API 序列化
```

完成后旧行为必须完全不变。

### Batch B：多主体检索

```text
subject scopes
subject hint
fact_key exact lookup
render_context 分组
decision_meta 增强
pgvector SQL 对齐
```

### Batch C：完整回合提取

```text
TurnMemoryExtractor
assistant candidate
shared candidate
assistant message source
echo suppression
```

### Batch D：冲突与一致性

```text
fact_key slot conflict
MemoryConsistencyGuard
repair once
回归测试
```

### Batch E：管理后台

```text
主体 tabs/filter
subject/fact_key columns
手动添加
冲突详情
检索调试
```

### Batch F：真实模型 P5 验收

使用至少 20 组跨会话稳定事实测试，包括：

```text
数字
日期
名字
偏好
共同约定
用户纠错
冲突
删除
L2 隔离
```

只在这一批通过后，开始正式 14 天观察期。

---

## 26. 预计修改范围

后端核心：

```text
server/app/db/models.py
server/alembic/versions/0010_memory_subject_scope.py
server/app/memory/models.py
server/app/memory/store.py
server/app/memory/extraction.py
server/app/memory/consolidation.py
server/app/memory/retrieval.py
server/app/chat/service.py
server/app/chat/memory_consistency.py
server/app/api/admin_memory.py
```

前端：

```text
web/apps/admin/src/views/MemoryView.vue
```

测试：

```text
server/tests/test_memory.py
server/tests/test_chat.py
server/tests/integration/test_memory_postgres.py
```

文档：

```text
docs/02-功能设计.md
docs/05-管理后台与可观测性.md
docs/21-记忆系统v1.md
docs/23-utility提取器与L2脱敏.md
docs/29-P5前置联调与配置运行时收口.md
TASKS.md
```

---

## 27. 架构决策

### ADR-MEM-01：事实不写进 Persona

Persona 是行为基线，不是长期事实数据库。聊天中形成的信息全部进入 MemorySystem。

### ADR-MEM-02：user_id 是 owner，不是 subject

保留现有 `user_id` 作为权限边界，新增 subject 描述记忆对象，避免破坏隔离语义。

### ADR-MEM-03：助手自述可以形成长期连续性

助手明确、稳定、非假设性的自述允许形成 assistant memory。它是“角色连续性事实”，不是现实世界证明。

### ADR-MEM-04：共同经历需要外部或用户证据

assistant 单方叙述不能创造 shared 现实事件，避免模型把幻觉固化成共同经历。

### ADR-MEM-05：槽位事实必须显式冲突

带 `fact_key` 的稳定属性不依赖向量阈值判定冲突，新值不得静默覆盖旧值。

### ADR-MEM-06：禁止记忆自我喂养

本轮由 memory 注入导致的 assistant 复述不是独立证据，不提升重要性，也不创建新 memory。

### ADR-MEM-07：角色连续性与现实能力分离

MemorySystem 决定“记得什么、自己是谁以及长期形成了哪些稳定自我信息”，RuntimeCapabilityProvider 决定“现在现实中真正能做什么”。助手的角色身体档案、生活偏好和自我叙述可以作为长期自我记忆自然表达，但不能因此推导出未接入的现实移动、触碰或设备执行能力。任何现实动作建议必须服从后者。

---

## 28. 最终验收定义

本设计真正完成，不以“数据库多了 subject 字段”为标准，而以用户体验为标准：

```text
第一次会话：
助手建立一个新的稳定自述事实 F。

第二个完全独立的新会话：
用户用不同措辞询问 F。
助手能够召回同一事实，而不是重新编造。

第三个会话：
用户提出与 F 冲突的新值。
系统保留冲突证据，不静默覆盖。

删除或纠错 F 后：
后续对话只使用当前 active 版本。
```

同时必须满足：

```text
不跨用户串记忆
不跨隐私边界出站
不把助手复述当新证据
不把角色设定当现实能力
不因记忆故障阻断聊天
```

达到这些条件后，才算“助手自身信息作为持久化记忆”真正完成。

