# Local development only. Replace every credential before a shared deployment.
POSTGRES_DB=aria
POSTGRES_USER=aria
POSTGRES_PASSWORD=change-me-postgres
MQTT_USERNAME=aria
MQTT_PASSWORD=change-me-mqtt
ARIA_LOG_LEVEL=INFO

