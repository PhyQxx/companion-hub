diff --git a/src/open_llm_vtuber/agent/agent_factory.py b/src/open_llm_vtuber/agent/agent_factory.py
index 4892a23..34cf94d 100644
--- a/src/open_llm_vtuber/agent/agent_factory.py
+++ b/src/open_llm_vtuber/agent/agent_factory.py
@@ -6,6 +6,7 @@ from .agents.basic_memory_agent import BasicMemoryAgent
 from .stateless_llm_factory import LLMFactory as StatelessLLMFactory
 from .agents.hume_ai import HumeAIAgent
 from .agents.letta_agent import LettaAgent
+from aria_olv_bridge.agent import AriaAgent
 
 from ..mcpp.tool_manager import ToolManager
 from ..mcpp.tool_executor import ToolExecutor
@@ -128,5 +129,23 @@ class AgentFactory:
                 port=settings.get("port"),
             )
 
+        elif conversation_agent_choice == "aria_agent":
+            settings = agent_settings.get("aria_agent", {})
+            return AriaAgent(
+                live2d_model=live2d_model,
+                tts_preprocessor_config=tts_preprocessor_config,
+                base_url=settings.get("base_url", "http://127.0.0.1:8000"),
+                password_env=settings.get("password_env", "ARIA_CHAT_PASSWORD"),
+                privacy_level=settings.get("privacy_level", "L1"),
+                conversation_title=settings.get(
+                    "conversation_title", "Open-LLM-VTuber"
+                ),
+                mapping_path=settings.get(
+                    "mapping_path", ".aria/open_llm_vtuber_conversations.json"
+                ),
+                faster_first_response=settings.get("faster_first_response", True),
+                segment_method=settings.get("segment_method", "pysbd"),
+            )
+
         else:
             raise ValueError(f"Unsupported agent type: {conversation_agent_choice}")
diff --git a/src/open_llm_vtuber/conversations/single_conversation.py b/src/open_llm_vtuber/conversations/single_conversation.py
index 493fa9c..d083dc2 100644
--- a/src/open_llm_vtuber/conversations/single_conversation.py
+++ b/src/open_llm_vtuber/conversations/single_conversation.py
@@ -68,7 +68,10 @@ async def process_single_conversation(
         )
 
         # Store user message (check if we should skip storing to history)
-        skip_history = metadata and metadata.get("skip_history", False)
+        skip_history = bool(
+            (metadata and metadata.get("skip_history", False))
+            or getattr(context.agent_engine, "manages_history", False)
+        )
         if context.history_uid and not skip_history:
             store_message(
                 conf_uid=context.character_config.conf_uid,
@@ -148,7 +151,11 @@ async def process_single_conversation(
             client_uid=client_uid,
         )
 
-        if context.history_uid and full_response:  # Check full_response before storing
+        if (
+            context.history_uid
+            and full_response
+            and not skip_history
+        ):  # Check full_response before storing
             store_message(
                 conf_uid=context.character_config.conf_uid,
                 history_uid=context.history_uid,
